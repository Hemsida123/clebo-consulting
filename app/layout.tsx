import type {Metadata} from "next"; import {headers} from "next/headers"; import "./globals.css";
export async function generateMetadata():Promise<Metadata>{const h=await headers();const host=h.get("x-forwarded-host")??h.get("host")??"localhost:3000";const protocol=h.get("x-forwarded-proto")??(host.startsWith("localhost")?"http":"https");const image=`${protocol}://${host}/og.png`;return{title:"Clebo Consulting | Expertgranskning inom familjerätt",description:"Oberoende, forskningsbaserad granskning av socialtjänstens utredningar i vårdnadstvister och LVU-ärenden.",openGraph:{title:"När barnets bästa kräver mer än en åsikt",description:"Expertgranskning inom familjerätt och socialt arbete.",images:[image]},twitter:{card:"summary_large_image",images:[image]}}}
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="sv"><body>{children}</body></html>}
